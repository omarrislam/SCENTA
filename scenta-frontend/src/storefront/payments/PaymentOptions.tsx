const PaymentOptions = () => (
  <div className="card">
    <h3 className="section-title">Payment</h3>
    <p>Cash on delivery is instant. For card payment, click Continue to card on the review step.</p>
  </div>
);

export default PaymentOptions;
